function(src, mod) {
                        var name = mod.name;
                        if ($.fn[name + type]) {
                            elem[name + type](problem, info);
                        }
                    }