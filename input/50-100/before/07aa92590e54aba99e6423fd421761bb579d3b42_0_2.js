function(index,x,y,rotation,scaleX,scaleY,alpha){

				

				objects[index].x = x;

				objects[index].y = y;

				objects[index].rotation = rotation;

				objects[index].scaleX = scaleX;

				objects[index].scaleY = scaleY;

				objects[index].alpha = alpha;

				

			}