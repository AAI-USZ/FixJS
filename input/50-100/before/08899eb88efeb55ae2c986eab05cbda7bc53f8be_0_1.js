function() {

                    this.copyObj.draw( this.canvas );

                    this.overlay.ctx.clearRect( 0, 0, this.width, this.height );

                    this.endDraw( clip );

                }