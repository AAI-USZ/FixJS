function(intIndex, entityId){
          console.log('connect', arguments);
          //var interactionId = model.get('_entities.' + intIndex + '.id');
          //fn(entityId, interactionId);
        }