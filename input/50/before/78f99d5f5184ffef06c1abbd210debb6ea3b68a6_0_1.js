function(entry) {
                            // use type coercion in case it's an int
                            if (entry.value == startingValue) {
                                self.setValue(startingValue);
                            }
                        }