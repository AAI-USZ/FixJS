function setupScene(storyboard){

//			createMeshes();
			//sovrapponiEffetti(animations);
		    init(640, 480, storyboard);

		    saveOriginalState();
		    meshesStartingState();
		    animate();

		}