function(deviceId, manufacturer, model, dmVersion, language) {
                return {
                    deviceId: deviceId,
                    manufacturer: manufacturer,
                    model: model,
                    dmVersion: dmVersion,
                    language: language,
                };
            }