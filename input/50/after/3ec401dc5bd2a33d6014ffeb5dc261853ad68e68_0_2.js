function() {
                            if (animate) {
                                container.css(animValues.property, "");
                            }
                            container.css("opacity", "");
                            hidden = false;
                        }