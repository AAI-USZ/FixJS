function(e) {
                if(!$('.dynamic-tip').length)
                    $('.default-tip').show();
                else
                    $('.wax-tooltip .close').text('Fechar');
            }