function writelast() { 
                                    // write the last bit, so we can know when it's finished
                                    pasvconn.write("\r\n", success);
                                }